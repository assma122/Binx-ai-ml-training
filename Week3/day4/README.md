# Day 4 - Trees, Forests, SVMs & k-NN

## Introduction

This notebook focuses on training and comparing different machine learning classification models using the same dataset and the same train/test split.

## Objective

The objective of this notebook is to:

- Train multiple classification models.
- Compare their performance using the same evaluation metrics.
- Analyze Random Forest feature importances.
- Identify the best-performing model.

## Dataset

- Dataset: Pima Indians Diabetes Dataset
- File: `diabetes.csv`
- Target Variable: `Outcome`

## Models Used

- Decision Tree
- Random Forest
- Support Vector Machine (SVM)
- k-Nearest Neighbors (k-NN)

## Topics Covered

- Decision Tree
- Random Forest
- Feature Importances
- Support Vector Machine (SVM)
- k-Nearest Neighbors (k-NN)
- Model Comparison
- Accuracy
- Precision
- Recall
- F1-score

## Hands-On Lab

- Step 1: Train a Decision Tree, Random Forest, SVM, and k-NN using the same train/test split.
- Step 2: Evaluate all models using the same metric and compare the results in one table.
- Step 3: Report the Random Forest feature importances and interpret them.
- Step 4: Identify the best-performing model and explain why it achieved the best performance.

## Visualizations

- Random Forest Feature Importance Chart
- F1-score Comparison Chart

## Tools Used

- Python
- Pandas
- NumPy
- Matplotlib
- Scikit-learn
- Jupyter Notebook

## Outcome

By the end of this lab, I trained and compared four classification models, evaluated their performance using the same metrics, analyzed the Random Forest feature importances, and identified the best-performing model.